#include "main.h"

/**
 * add - adds two numbers
 * @i: parameter to be checked
 * @n: parameter to be checked
 *
 * Return: sum  always
 */
int add(int i, int n)
{
	int sum;

	sum = i + n;

	return (sum);
}
