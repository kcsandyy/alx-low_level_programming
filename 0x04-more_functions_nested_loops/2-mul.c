#include "main.h"
/**
 * mul - multiplies two integers
 * @a: - parameter to be checked
 * @b: - parameter to be checked
 * Return: always 0
 */
int mul(int a, int b)
{
	int result = a * b;

	return (result);
}
